lammps:
  description: Deployable LAMMPS image
  requires:
    - python_deploy_base
  build: |
    RUN apt-get update \
     && apt-get install -y lammps ssh \
     && cleanapt
  #test_directory: buildfiles/lammps/tests
  #test: |
  #   cd colloid && lammps < colloid.in
  #   cd threebody && lammps < in.threebody

